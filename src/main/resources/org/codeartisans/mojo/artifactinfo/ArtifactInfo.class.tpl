package #packageName#;

/**
 * Generated class with information from the POM.
 */
public abstract class #className#
{
    /** Private constructor to prevent instantiation. */
    private #className#() {
        // Private constructor to prevent instantiation.
    }
    /** groupId read from the POM. */
    public final static String GROUPID = "#groupId#";
    /** artifactId read from the POM. */
    public final static String ARTIFACTID = "#artifactId#";
    /** version read from the POM. */
    public final static String VERSION = "#version#";
    /** description read from the POM. */
    public final static String DESCRIPTION = "#description#";
    /** name read from the POM. */
    public final static String NAME = "#name#";
    /** url read from the POM. */
    public final static String URL = "#url#";
    /** inception year read from the POM. */
    public final static String INCEPTION_YEAR = "#inceptionYear#";
    /** epoch of the build. */
    public final static long BUILD_TIMESTAMP = #buildTimestamp#L;

}
